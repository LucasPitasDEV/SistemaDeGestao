<UserControl x:Class="SistemaDeGestao.Views.PessoaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:SistemaDeGestao.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="LightGray" BorderThickness="1" Padding="15" CornerRadius="5" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Cadastro de Pessoas" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Nome:" VerticalAlignment="Center" Width="80"/>
                    <TextBox Width="200" Text="{Binding Nome}"/>
                    <TextBlock Text="CPF:" VerticalAlignment="Center" Width="80" Margin="20,0,0,0"/>
                    <TextBox Width="200" 
                    Text="{Binding Cpf, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" views:PessoaView.CpfMaskedText="{Binding Cpf}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Endereço:" VerticalAlignment="Center" Width="80"/>
                    <TextBox Width="420" Text="{Binding Endereco}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Novo" Width="80" Margin="5,0" Command="{Binding NovaPessoaCommand}"/>
                    <Button Content="Salvar" Width="80" Margin="5,0" Command="{Binding SalvarPessoaCommand}"/>
                    <Button Content="Excluir" Width="80" Margin="5,0" Command="{Binding ExcluirPessoaCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Pesquisar Pessoas" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Nome:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Width="150" Text="{Binding NomeFiltro}"/>
                <TextBlock Text="CPF:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                <TextBox Width="150" Text="{Binding CpfFiltro}"/>
                <Button Content="Buscar" Width="80" Margin="10,0,0,0" Command="{Binding BuscarPessoasCommand}"/>
            </StackPanel>

            <DataGrid ItemsSource="{Binding Pessoas}"
                      SelectedItem="{Binding PessoaSelecionada}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Height="250"
                      Margin="0,10,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                    <DataGridTextColumn Header="CPF" Binding="{Binding Cpf}" Width="150"/>
                    <DataGridTextColumn Header="Endereço" Binding="{Binding Endereco}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>