<UserControl x:Class="SistemaDeGestao.Views.ProdutoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="LightGray" BorderThickness="1" Padding="15" CornerRadius="5" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Cadastro de Produtos" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Nome:" VerticalAlignment="Center" Width="80"/>
                    <TextBox Width="200" Text="{Binding Nome}"/>
                    <TextBlock Text="Código:" VerticalAlignment="Center" Width="80" Margin="20,0,0,0"/>
                    <TextBox Width="200" Text="{Binding Codigo}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Valor (Ex.R$0.00):" VerticalAlignment="Center" Width="90"/>
                    <TextBox Width="100" Text="{Binding Valor}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="Novo" Width="80" Margin="5,0" Command="{Binding NovoProdutoCommand}"/>
                    <Button Content="Salvar" Width="80" Margin="5,0" Command="{Binding SalvarProdutoCommand}"/>
                    <Button Content="Excluir" Width="80" Margin="5,0" Command="{Binding ExcluirProdutoCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1">
            <TextBlock Text="Pesquisar Produtos" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Nome:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox Width="150" Text="{Binding NomeFiltro}"/>
                <TextBlock Text="Código:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                <TextBox Width="150" Text="{Binding CodigoFiltro}"/>
                <TextBlock Text="Valor Min.:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                <TextBox Width="80" Text="{Binding ValorMinFiltro}"/>
                <TextBlock Text="Valor Máx.:" VerticalAlignment="Center" Margin="10,0,5,0"/>
                <TextBox Width="80" Text="{Binding ValorMaxFiltro}"/>
                <Button Content="Buscar" Width="80" Margin="10,0,0,0" Command="{Binding BuscarProdutosCommand}"/>
            </StackPanel>

            <DataGrid ItemsSource="{Binding Produtos}"
                      SelectedItem="{Binding ProdutoSelecionado}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      Height="463"
                      Margin="0,10,0,0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                    <DataGridTextColumn Header="Código" Binding="{Binding Codigo}" Width="150"/>
                    <DataGridTextColumn Header="Valor" Binding="{Binding Valor, StringFormat=C2}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>