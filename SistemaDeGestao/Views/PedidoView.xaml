<UserControl x:Class="SistemaDeGestao.Views.PedidoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="LightGray" BorderThickness="1" Padding="15" CornerRadius="5" Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Novo Pedido" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Cliente:" VerticalAlignment="Center" Width="100"/>
                    <ComboBox Width="250"
                              ItemsSource="{Binding Pessoas}"
                              DisplayMemberPath="Nome"
                              SelectedItem="{Binding PessoaSelecionada}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Produto:" VerticalAlignment="Center" Width="100"/>
                    <ComboBox Width="250"
                              ItemsSource="{Binding Produtos}"
                              DisplayMemberPath="Nome"
                              SelectedItem="{Binding ProdutoParaAdicionar}"/>
                    <TextBlock Text="Quantidade:" VerticalAlignment="Center" Margin="20,0,5,0"/>
                    <TextBox Width="50" Text="{Binding QuantidadeParaAdicionar}"/>
                    <Button Content="Adicionar" Width="100" Margin="20,0,0,0" Command="{Binding AdicionarItemCommand}"/>
                </StackPanel>

                <DataGrid ItemsSource="{Binding ItensDoPedido}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          Height="150"
                          Margin="0,10,0,10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Produto" Binding="{Binding Produto.Nome}" Width="*"/>
                        <DataGridTextColumn Header="Quantidade" Binding="{Binding Quantidade}" Width="100"/>
                        <DataGridTextColumn Header="Valor Unit." Binding="{Binding Produto.Valor, StringFormat=C2}" Width="100"/>
                        <DataGridTextColumn Header="Valor Total" Binding="{Binding ValorTotal, StringFormat=C2}" Width="100"/>
                        <DataGridTemplateColumn Width="70">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Remover" Command="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}}, Path=DataContext.RemoverItemCommand}" CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <TextBlock Text="Valor Total:" FontWeight="Bold" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="{Binding ValorTotal, StringFormat=C2}" FontWeight="Bold" FontSize="16" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                    <TextBlock Text="Forma de Pgto.:" VerticalAlignment="Center" Width="120"/>
                    <ComboBox Width="200"
                              ItemsSource="{Binding FormasPagamento}"
                              SelectedItem="{Binding FormaPagamentoSelecionada}"/>
                    <Button Content="Finalizar Pedido" Width="150" Margin="30,0,0,0" Command="{Binding FinalizarPedidoCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>

    </Grid>
</UserControl>